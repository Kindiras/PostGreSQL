SELECT Min(replacement_cost), MAX(replacement_cost),COUNT(replacement_cost),SUM(replacement_cost),ROUND(AVG(replacement_cost),3) FROM film
--WHERE first_name ILIKE 'j%' AND last_name ILIKE 'j%'
--ORDER BY amount DESC
--WHERE first_name LIKE '_%er%' AND last_name NOT LIKE '%B'
--ORDER BY last_name DESC
LIMIT 5;
